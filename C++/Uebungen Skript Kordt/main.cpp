#include "Uebungen Funx.h"

int main()
{
    char string[81];
    int anz = 0;
    cout << "Gib einen String (max 80 Zeichen) ein, den du veraendern moechtest." << endl;
    cin.getline(string, 80);

    // uebung3(string);
reversestring(string);



    return 0;
}
