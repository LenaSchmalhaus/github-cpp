#include "funktionen.h"


int main()
{

    // Inhalt eines Arrays verdoppeln:

/*
    int arr[6] = {1, 2, 3, 4, 5, 6};  // Das Array

    // Rufe die Funktion auf, um das Array zu verdoppeln
    arrayverdoppeln(arr,6);

    // Gib das verdoppelte Array aus
    for (int i = 0; i < 6; i++) {
        cout << arr[i] << " ";
    }
*/



//Pointer auf Strings übergeben:
//Schreibe eine Funktion, die einen String (als char-Array) entgegennimmt und jedes Zeichen des Strings einzeln ausgibt.

char text[]={"Beeblebrox"};
    zeichenausgeben(text);

    cout << text << endl;












    return 0;
}
