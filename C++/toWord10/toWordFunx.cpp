#include "toWordFunx.h"


void toWord(unsigned long wert, char*wort)
{
    int ergebnis;
    unsigned long rest;

int dezimal ()
{

}





if ( wert >= 1000000000)
{
    int ergebnis = wert / 1000000000;
    unsigned long rest = wert % 1000000000;
}

    if ( wert >= 1000000)
{
    int ergebnis = wert / 1000000;
    unsigned long rest = wert % 1000000;
}

    if ( wert >= 100000)
{
    int ergebnis = wert / 100000;
    unsigned long rest = wert % 100000;
}
    if ( wert >= 10000)
{
    int ergebnis = wert / 10000;
    unsigned long rest = wert % 10000;
}

    if ( wert >= 1000)
{
    int ergebnis = wert / 1000;
    unsigned long rest = wert % 1000;
}

    if ( wert >= 100)
{
    int ergebnis = wert / 100;
    unsigned long rest = wert % 100;
}

    if ( wert >= 10)
{
    int ergebnis = wert / 10;
    unsigned long rest = wert % 10;
}



}
